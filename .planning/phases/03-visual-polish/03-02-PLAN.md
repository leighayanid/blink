---
phase: 03-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [app/pages/index.vue, app.vue]
autonomous: true
---

<objective>
Redesign main page layout with Linear-inspired header, content sections, and responsive grid.

Purpose: Transform the main page from functional to premium. Bold header, clean sections, proper visual hierarchy.
Output: Completely restyled index.vue with new layout structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-polish/03-CONTEXT.md
@.planning/phases/03-visual-polish/03-01-SUMMARY.md

@app/pages/index.vue
@app.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign header and app shell</name>
  <files>app/pages/index.vue</files>
  <action>
    Redesign the header section in index.vue:

    1. **Hero header** (Linear-inspired):
      - Large, bold app name "Blink" - use gradient text (orange to amber)
       - Remove emoji from title (cleaner look)
       - Tagline in muted text below
       - Add subtle background gradient (very subtle orange tint at top fading to transparent)

    2. **Local device card** - premium redesign:
       - Full-width card with glassmorphism effect (backdrop-blur, semi-transparent bg)
       - Device icon larger, with subtle glow effect
       - Connection status as pill badge (green pulse for connected)
       - Use design system card-interactive class

    3. **Section headers**:
       - Consistent h2 styling with small caps or letter-spacing
       - Add subtle divider line or gradient underline
       - Badge count styled as pill (e.g., "Available Devices (3)")

    4. **Overall layout**:
       - Max-width container centered
       - Generous vertical spacing between sections (use --space-8)
       - Sections use card-base styling from design system
  </action>
  <verify>`npm run dev` renders updated header; no console errors</verify>
  <done>Header redesigned with gradient title, glassmorphism local device card, premium section headers</done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive layout with CSS Grid</name>
  <files>app/pages/index.vue</files>
  <action>
    Update the main content area for responsive design:

    1. **Desktop (>1024px)**:
       - Two-column layout: left side (Your Device, Available Devices), right side (Connected, Send Files, Progress)
       - Use CSS Grid: `grid-template-columns: 1fr 1fr` with gap

    2. **Tablet (768px-1024px)**:
       - Keep two columns but reduce gap
       - Slightly smaller font sizes

    3. **Mobile (<768px)**:
       - Single column stack
       - Full-width cards
       - Header becomes more compact
       - Touch-friendly spacing (min 44px tap targets)

    4. **Transitions**:
       - Smooth layout transitions on resize (transition on gap, padding)
       - Cards animate in with fadeIn/slideUp on mount (use design system keyframes)

    Update the scoped styles to use:
    - CSS Grid for main layout
    - Media queries for breakpoints
    - Design system variables for spacing and transitions
  </action>
  <verify>`npm run dev`; resize browser window to test all breakpoints; layout responds correctly</verify>
  <done>Responsive CSS Grid layout working at desktop, tablet, and mobile breakpoints</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] Desktop: Two-column layout renders correctly
- [ ] Mobile: Single column stacks properly
- [ ] No horizontal scroll at any viewport size
- [ ] Header looks premium with gradient and proper spacing
</verification>

<success_criteria>
- Header redesigned with Linear-inspired premium look
- Responsive grid working at all breakpoints
- Uses design system variables consistently
- No visual regressions in functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-polish/03-02-SUMMARY.md`
</output>
