// This file is kept for backwards compatibility.
// The canonical shared types live in packages/types (@blink/types).
// Import from '@blink/types' directly in all new code.
export type { Device, Transfer, FileMetadata, ChunkData, SignalingMessage } from '@blink/types'
