# ─────────────────────────────────────────────────────────────────────────────
# Blink Web App — Environment Variables
# Copy this file to .env and fill in the values for your environment.
# ─────────────────────────────────────────────────────────────────────────────

# WebSocket URL used by the signaling server (server-side rendering context).
# Browser clients automatically derive the URL from window.location.host,
# so this variable only matters for SSR or reverse-proxy configurations.
WS_URL=ws://localhost:3000

# HTTP base URL of the signaling server (reserved for future use).
SIGNALING_SERVER=http://localhost:3000
